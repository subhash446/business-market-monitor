/**
 * TODO (Email Alerts phase — Document 3 §10): node-cron schedule using
 * config/env.js's cron.alertEvaluation (ALERT_EVALUATION_CRON). Calls
 * alertEvaluation.service.js + notification.service.js (channel-agnostic).
 * Not scheduled yet — no node-cron dependency installed until this phase.
 */
module.exports = {};
