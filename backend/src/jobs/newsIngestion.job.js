/**
 * TODO (Market News phase — Document 3 §9): node-cron schedule using
 * config/env.js's cron.newsIngestion (NEWS_INGESTION_CRON). Calls
 * newsIngestion.service.js + newsTagging.service.js.
 * Not scheduled yet — no node-cron dependency installed until this phase.
 */
module.exports = {};
